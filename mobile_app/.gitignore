# Flutter/Dart generated
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
dist/

# Android generated (GitHub Actions creates these fresh each build)
android/.gradle/
android/gradle/
android/gradle.properties
android/gradlew
android/gradlew.bat
android/local.properties
android/settings.gradle
android/build.gradle
android/app/build.gradle
android/app/src/main/kotlin/
android/app/src/main/java/

# Kotlin-DSL equivalents of the above. Current Flutter generates .kts, not
# Groovy, so the names above no longer match what `flutter create` leaves
# behind and the files were showing up as untracked.
android/settings.gradle.kts
android/build.gradle.kts
android/app/build.gradle.kts
android/.gitignore
android/app/src/debug/
android/app/src/profile/
android/app/src/main/res/

# Regenerated by `flutter create` / tooling each build
.metadata


# iOS (not used)
ios/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db
